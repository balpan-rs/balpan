#[cfg(test)]
mod integration_test {
    use balpan::config::*;  // load specific source code

    // placeholder test
    #[test]
    fn test_default_lang_config() {
        // let default_config = include_bytes!("../languages.toml");
        // let default_config = toml::from_str(std::str::from_utf8(default_config).unwrap()).expect("Could not parse built-in languages.toml to valid toml");
        // assert_eq!(default_lang_config(), default_config);
    }
}